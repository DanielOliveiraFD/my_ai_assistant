# Copie este arquivo para .env e preencha com sua chave real.
# .env nunca deve ser commitado (já está no .gitignore).

GROQ_API_KEY=sua_chave_aqui

# Busca na web (jarvis/websearch/) — crie uma chave grátis em tavily.com
TAVILY_API_KEY=sua_chave_aqui
